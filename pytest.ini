[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --cov=agents
    --cov=services
    --cov=db
    --cov=config
    --cov-report=term-missing
    --cov-report=html
    -v

